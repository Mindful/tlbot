
# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Index page just confirms the server is alive
GET     /                           controllers.HomeController.index
POST    /botMessage                 controllers.BotController.receiveMessage

      